# saltOS default chrony configuration.
pool pool.ntp.org iburst
driftfile /var/lib/chrony/drift
# Step the clock on the first updates if it is far off (e.g. no RTC battery).
makestep 1.0 3
# Keep the hardware clock in sync (kernel 11-minute mode).
rtcsync
