name = "qtermwidget"
version = "2.1.0"
release = 1
summary = "Qt terminal emulator widget library"
license = "GPL-2.0-or-later"
arch = ["x86_64", "aarch64"]

[source]
url = "https://github.com/lxqt/qtermwidget/releases/download/2.1.0/qtermwidget-2.1.0.tar.xz"
sha256 = "TODO-sha256"

[build]
system = "cmake"
deps = ["cmake", "ninja", "pkgconf", "qt6-base", "lxqt-build-tools", "gcc"]

[package]
deps = ["glibc", "qt6-base"]

[reproducibility]
status = "verified"
