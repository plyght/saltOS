name = "menu-cache"
version = "1.1.0"
release = 1
summary = "Caching mechanism for freedesktop.org menus"
license = "LGPL-2.1-or-later"
arch = ["x86_64", "aarch64"]

[source]
url = "https://github.com/lxde/menu-cache/releases/download/1.1.0/menu-cache-1.1.0.tar.xz"
sha256 = "TODO-sha256"

[build]
system = "autotools"
deps = ["pkgconf", "gcc", "make", "glib"]

[package]
deps = ["glibc", "glib"]

[reproducibility]
status = "verified"
