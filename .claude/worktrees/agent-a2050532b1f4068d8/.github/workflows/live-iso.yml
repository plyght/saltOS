name: live-iso

on:
  workflow_dispatch:
    inputs:
      edition:
        description: "console or desktop"
        default: "desktop"
  push:
    paths:
      - "os/iso/live-build.sh"
      - ".github/workflows/live-iso.yml"

jobs:
  build:
    name: live ISO (x86_64, ${{ matrix.label }})
    runs-on: ubuntu-latest
    continue-on-error: ${{ matrix.edition == 'desktop' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { edition: console, e2e: "0", label: console }
          - { edition: console, e2e: "1", label: e2e }
          - { edition: desktop, e2e: "0", label: desktop }
    env:
      EDITION: ${{ matrix.edition }}
      SALTOS_E2E: ${{ matrix.e2e }}
    steps:
      - uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/local/lib/android /opt/ghc /usr/share/dotnet \
            /opt/hostedtoolcache/CodeQL /usr/local/.ghcup 2>/dev/null || true
          df -h /

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build pkg-config \
            libzstd-dev libsodium-dev libsqlite3-dev build-essential \
            mmdebstrap debian-archive-keyring squashfs-tools xorriso mtools \
            grub-pc-bin grub-efi-amd64-bin grub-common \
            qemu-system-x86 ovmf socat netpbm

      - name: Build salt against the Debian base
        run: |
          docker run --rm -v "$PWD":/src -w /src debian:bookworm bash -c '
            set -e
            apt-get update
            apt-get install -y --no-install-recommends \
              cmake ninja-build pkg-config g++ ca-certificates \
              libzstd-dev libsodium-dev libsqlite3-dev
            cmake -G Ninja -B build-iso -DCMAKE_BUILD_TYPE=Release
            cmake --build build-iso --target salt'

      - name: Build live ISO
        run: |
          sudo env \
            SALT_BIN="$PWD/build-iso/src/salt/salt" \
            REPO_DIR="$PWD" \
            EDITION="$EDITION" \
            SALTOS_E2E="$SALTOS_E2E" \
            VERSION="0.1.0" \
            OUT="$PWD/out-iso" \
            bash os/iso/live-build.sh x86_64
          sudo chown -R "$USER" out-iso

      - name: Boot test (QEMU)
        run: |
          ISO=$(ls out-iso/*.iso | head -1)
          echo "boot-testing $ISO (edition=$EDITION e2e=$SALTOS_E2E)"
          rm -f /tmp/qmon
          NET=""
          if [ "$SALTOS_E2E" = "1" ]; then
            MARKER="SALTOS_STRATUM_E2E_OK"; MEM=2560; TMO=540; TRIES=53; NET="-nic user,model=e1000"
          elif [ "$EDITION" = "desktop" ]; then
            MARKER="SALTOS_DESKTOP_OK"; MEM=4096; TMO=420; TRIES=33
          else
            MARKER="SALTOS_BOOT_OK"; MEM=2560; TMO=420; TRIES=40
          fi
          set +e
          timeout "$TMO" qemu-system-x86_64 \
            -machine pc -m "$MEM" -vga std -display none -no-reboot $NET \
            -serial file:qemu.log -monitor unix:/tmp/qmon,server,nowait \
            -cdrom "$ISO" -boot d &
          QPID=$!
          for i in $(seq 1 "$TRIES"); do
            sleep 10
            grep -q "$MARKER" qemu.log 2>/dev/null && break
            grep -q "SALTOS_STRATUM_E2E_FAIL" qemu.log 2>/dev/null && break
          done
          if [ "$EDITION" = "desktop" ]; then
            sleep 15
            printf 'screendump /tmp/screen.ppm\n' | socat - unix-connect:/tmp/qmon || true
            sleep 3
            [ -f /tmp/screen.ppm ] && pnmtopng /tmp/screen.ppm > saltos-desktop.png || echo "no screendump"
          else
            sleep 3
          fi
          kill $QPID 2>/dev/null || true
          set -e
          echo "----- qemu serial tail -----"
          tail -n 80 qemu.log || true
          if grep -q "$MARKER" qemu.log; then
            echo "BOOT OK: found $MARKER"
          else
            echo "BOOT FAILED: $MARKER not found"
            exit 1
          fi
          if [ "$EDITION" != "desktop" ]; then
            if grep -q "SALTOS_STRATUM_OK" qemu.log; then
              echo "STRATUM PLANE OK: found SALTOS_STRATUM_OK"
            else
              echo "STRATUM PLANE FAILED: SALTOS_STRATUM_OK not found"
              exit 1
            fi
          fi

      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: saltos-desktop-screenshot
          path: saltos-desktop.png
          if-no-files-found: ignore

      - name: Upload serial log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-serial-log
          path: qemu.log
          if-no-files-found: ignore

      - name: Upload ISO
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: saltos-live-x86_64-${{ matrix.edition }}
          path: out-iso/*.iso
          if-no-files-found: warn
